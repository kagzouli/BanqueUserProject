import { DisplaycolorbalanceDirective } from './displaycolorbalance.directive';

describe('DisplaycolorbalanceDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplaycolorbalanceDirective();
    expect(directive).toBeTruthy();
  });
});
